version: '2'
services:
  conferencega-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/conferenceGA/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=conferenceGA
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
